apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "shisha-backend.fullname" . }}
  labels:
    {{- include "shisha-backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "shisha-backend.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "shisha-backend.name" . }}
        chart: {{ include "shisha-backend.chart" . }}
    spec:
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      containers:
        - name: backend
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          readinessProbe:
            httpGet:
              path: {{ .Values.readinessProbe.path }}
              port: {{ .Values.readinessProbe.port }}
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
          livenessProbe:
            httpGet:
              path: {{ .Values.livenessProbe.path }}
              port: {{ .Values.livenessProbe.port }}
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: DATABASE_HOST
              value: "{{ .Values.env.database.host }}"
            - name: DATABASE_PORT
              value: "{{ .Values.env.database.port }}"
            - name: DATABASE_NAME
              value: "{{ .Values.env.database.name }}"
            - name: DATABASE_USER
              value: "{{ .Values.env.database.user }}"
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.database.passwordSecret }}
                  key: password
            - name: STORAGE
              value: "{{ .Values.env.storage.mode | default "couchdb" }}"
            - name: COUCHDB_URL
              value: "{{ .Values.env.storage.couchdb.url | default "http://couchdb:5984" }}"
            - name: COUCHDB_DATABASE
              value: "{{ .Values.env.storage.couchdb.database | default "shisha" }}"
 
            {{- if .Values.env.storage.couchdb.adminSecretName }}
            - name: COUCHDB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.storage.couchdb.adminSecretName }}
                  key: username
            - name: COUCHDB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.storage.couchdb.adminSecretName }}
                  key: password
            {{- else }}
            - name: COUCHDB_USER
              value: ""
            - name: COUCHDB_PASSWORD
              value: ""
            {{- end }}
      affinity:
        {{- toYaml .Values.affinity | nindent 8 }}