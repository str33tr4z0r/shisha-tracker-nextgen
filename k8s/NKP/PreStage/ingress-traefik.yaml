# Haupt-Ingress: Frontend + direkte API-Routen
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shisha-ingress
  namespace: shisha
  annotations:
    # optional: auf welchen EntryPoint Traefik lauscht (web=80, websecure=443)
    #traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: kommander-traefik
  rules:
    - http:
        paths:
          - path: /shisha
            pathType: Prefix
            backend:
              service:
                name: shisha-frontend
                port:
                  number: 80
          - path: /shisha/api
            pathType: Prefix
            backend:
              service:
                name: shisha-frontend
                port:
                  number: 80
          - path: /shisha/api/shishas
            pathType: Prefix
            backend:
              service:
                name: shisha-frontend
                port:
                  number: 80
          - path: /shisha/api/info
            pathType: Prefix
            backend:
              service:
                name: shisha-frontend
                port:
                  number: 80
          - path: /shishaapi/db-health
            pathType: Prefix
            backend:
              service:
                name: shisha-backend-mock
                port:
                  number: 8080
          - path: /shishaapi/healthz
            pathType: Prefix
            backend:
              service:
                name: shisha-backend-mock
                port:
                  number: 8080
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: health-db-rewrite
  namespace: shisha
spec:
  replacePathRegex:
    regex: ^/shisha/api/health/db(?:/|$)(.*)
    replacement: /shisha/api/db-health/$1
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: healthz-db-rewrite
  namespace: shisha
spec:
  replacePathRegex:
    regex: ^/shisha/api/healthz/db(?:/|$)(.*)
    replacement: /shisha/api/healthz/$1
---
# Ingress-Route, die das Middleware-Rewrite nutzt
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shisha-ingress-health-rewrite
  namespace: shisha
  annotations:
    # Middleware-Referenz: <namespace>-<middlewareName>@kubernetescrd
    traefik.ingress.kubernetes.io/router.middlewares: shisha-health-db-rewrite@kubernetescrd
spec:
  ingressClassName: kommander-traefik
  rules:
    - http:
        paths:
          - path: /shisha/api/health/db
            pathType: Prefix
            backend:
              service:
                name: shisha-backend-mock
                port:
                  number: 8080
---
# Ingress-Route für das zweite Rewrite
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shisha-ingress-healthz-rewrite
  namespace: shisha
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: shisha-healthz-db-rewrite@kubernetescrd
spec:
  ingressClassName: kommander-traefik
  rules:
    - http:
        paths:
          - path: /shisha/api/healthz/db
            pathType: Prefix
            backend:
              service:
                name: shisha-backend-mock
                port:
                  number: 8080
